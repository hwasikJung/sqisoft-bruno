info:
  name: 내 프로필 수정
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/v1/users/me
  body:
    type: json
    data: |
      {
        "userNm": "수정된이름"
      }
  auth: inherit

headers:
  - name: Content-Type
    value: application/json
    enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
