info:
  name: 내 프로필 조회
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/v1/users/me
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
