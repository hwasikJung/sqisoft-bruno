info:
  name: 사용자 상태 수정
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8080/adm/v1/users/admin@test.com/status
  body:
    type: json
    data: |
      {
        "userSttsCd": "A"
      }
  auth: inherit

headers:
  - name: Content-Type
    value: application/json
    enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
