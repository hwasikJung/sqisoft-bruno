info:
  name: 비밀번호 초기화
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/adm/v1/users/admin@test.com/password/reset
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
