info:
  name: 사용자 역할 수정
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/adm/v1/users/admin@test.com/role
  body:
    type: json
    data: |
      {
        "roleId": "ROLE_ADMIN"
      }
  auth: inherit

headers:
  - name: Content-Type
    value: application/json
    enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
